import { ImenikFilterPipe } from './imenik-filter.pipe';

describe('ImenikFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ImenikFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
